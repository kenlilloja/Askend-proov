package askend.classifier.repository;

import askend.classifier.model.ClassifierValue;
import askend.core.repository.BaseRepository;

public interface ClassifierValueRepository extends BaseRepository<ClassifierValue, Long> {
}
